apiVersion: v1
kind: Service
metadata:
  name: mern-frontend-service
spec:
  selector:
    app: mern-frontend   # must match your frontend Deployment labels
  ports:
    - protocol: TCP
      port: 80           # exposed to cluster
      targetPort: 80     # container port
  type: ClusterIP