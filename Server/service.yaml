apiVersion: v1
kind: Service
metadata:
  name: mern-backend-service
spec:
  type: LoadBalancer
  selector:
    app: mern-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP    